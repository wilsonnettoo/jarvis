# =====================================================================
# Jarvis — variáveis de ambiente
# Copie este arquivo para `.env` e preencha as chaves que for usar.
# Você NÃO precisa preencher todas: só as do provedor que vai usar.
# =====================================================================

# ---------------------------------------------------------------------
# Provedor e modelo do "cérebro" (LLM)
# O Jarvis usa LiteLLM, então o modelo segue o formato do LiteLLM.
# Exemplos de JARVIS_MODEL:
#   OpenAI   -> gpt-4o            ou  gpt-4o-mini
#   Claude   -> claude-sonnet-4-6 (Anthropic)
#   Gemini   -> gemini/gemini-1.5-pro
#   Ollama   -> ollama/llama3.1   (local, precisa do Ollama rodando)
# ---------------------------------------------------------------------
JARVIS_MODEL=gpt-4o-mini

# Temperatura do modelo (0.0 = mais determinístico, 1.0 = mais criativo)
JARVIS_TEMPERATURE=0.4

# ---------------------------------------------------------------------
# Chaves de API (preencha apenas as que for usar)
# ---------------------------------------------------------------------
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Ollama roda local; normalmente não precisa de chave.
# Descomente se seu Ollama estiver em outro host/porta:
# OLLAMA_API_BASE=http://localhost:11434

# ---------------------------------------------------------------------
# Banco de dados (ferramenta consultar_postgres; opcional)
# Ex.: postgresql://usuario:senha@localhost:5432/meubanco
# Requer: pip install 'psycopg[binary]'
# ---------------------------------------------------------------------
DATABASE_URL=

# ---------------------------------------------------------------------
# Voz (modo --voz)
# ---------------------------------------------------------------------
# Motor de TTS:
#   "xtts"       -> voz CLONADA local/grátis (precisa de JARVIS_VOICE_SAMPLE)
#   "elevenlabs" -> voz clonada na nuvem (precisa de ELEVENLABS_*)
#   "openai"     -> vozes prontas (usa OPENAI_API_KEY)
#   "say"        -> macOS, robótico mas offline
JARVIS_TTS_ENGINE=openai
# Modelo OpenAI de TTS: tts-1 (rápido) ou tts-1-hd (maior qualidade).
JARVIS_TTS_MODEL=tts-1
# Voz OpenAI: alloy, echo, fable, onyx, nova, shimmer.
JARVIS_OPENAI_VOICE=nova
# Voz do macOS `say` (fallback): ex. Luciana, Eddy, Sandy (todas pt_BR).
JARVIS_TTS_VOICE=Luciana
# Tamanho do modelo STT (faster-whisper): tiny/base/small/medium/large-v3.
JARVIS_STT_MODEL=small

# --- Voz clonada ---
# Caminho de uma amostra de áudio (.wav/.mp3, ~6-30s) para clonar (XTTS).
JARVIS_VOICE_SAMPLE=app/storage/voz_sample.wav
# ElevenLabs (clonagem na nuvem): crie em elevenlabs.io
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
ELEVENLABS_MODEL=eleven_multilingual_v2

# Hotword (fase futura): chave grátis em console.picovoice.ai
PICOVOICE_ACCESS_KEY=

# ---------------------------------------------------------------------
# Comportamento de segurança
# Se "true", ações de risco MÉDIO e ALTO sempre pedem confirmação.
# ---------------------------------------------------------------------
JARVIS_REQUIRE_CONFIRMATION=true
