# =====================================================================
# Jarvis — dependências
# Etapa 0 (fundação texto-only). As dependências de voz estão listadas
# mais abaixo, comentadas, para serem habilitadas na fase de voz.
# =====================================================================

# --- Núcleo (cérebro, config, terminal) ---
litellm>=1.50            # gateway único para OpenAI/Claude/Gemini/Ollama
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
rich>=13.7               # terminal bonito (REPL do Jarvis)
PyYAML>=6.0              # perfil de memória estruturada

# --- Testes ---
pytest>=8.0
pytest-asyncio>=0.23

# --- Banco de dados (opcional; só se for usar consultar_postgres) ---
# psycopg[binary]>=3.1

# --- Voz (modo --voz) ---
sounddevice>=0.4           # captura de áudio do microfone
faster-whisper>=1.0        # STT local (fala -> texto), grátis e offline
numpy>=1.26                # manipulação do buffer de áudio
# TTS usa o comando `say` do macOS (nativo, sem dependência Python).

# =====================================================================
# VOZ — fase futura (hotword), exige chave Picovoice.
# =====================================================================
# pvporcupine>=3.0         # hotword "Jarvis" (Picovoice)
# webrtcvad>=2.0           # VAD mais robusto (hoje usamos energia/RMS)
# openai>=1.40             # alternativa: STT/TTS/Realtime via OpenAI
